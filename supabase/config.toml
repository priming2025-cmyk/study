# Supabase CLI 로컬/연동용 설정.
# https://supabase.com/docs/guides/local-development/cli/config
#
# 원격 프로젝트 ref는 `apps/mobile/.env` 의 SUPABASE_URL (호스트 앞부분) 또는
# 환경변수 SUPABASE_PROJECT_REF 로 `npm run db:push` 가 읽습니다.
# 비밀번호·DB URL은 이 파일에 넣지 마세요.

project_id = "setudy"
